{
  "name": "manhole-inspection-app",
  "version": "1.0.0",
  "description": "マンホールポンプ点検アプリ",
  "main": "index.html",
  "scripts": {
    "build": "echo 'Build completed'",
    "android": "npx cap run android",
    "android:build": "npx cap build android"
  },
  "devDependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  }
}